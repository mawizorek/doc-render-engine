# URITP - the first REAL site on the matrix publishing path.
#
# Distinct from `template` in the one way that matters: this one is published
# by build.yml into its own content repo's gh-pages branch, at its own address,
# for its own audience. The template is served from the engine's Pages as a
# special case; this is the ordinary path every real site uses.

name: URITP Production Resources
description: Venue information, technical standards, and production reference.
slug: uritp
base_url: https://mawizorek.github.io/uritp-docs/

content_repo: mawizorek/uritp-docs
content_branch: main

# THE TOGGLE CYCLES TWO THEMES, AND EACH IS ONE COMPLETE PALETTE (2026-08-04).
#
# `theme:` accepts either shape. A bare slug paints both colour schemes from one
# theme, which is still the common case -- template, theatre and hml all use it:
#
#     theme: eos
#
# A MAP names a theme per scheme, so the light/dark toggle cycles two different
# identities rather than two halves of one row:
#
#     theme:
#       dark: mclaren
#       light: eos-light
#
# WARNING: NAME THE ROW, NOT THE IDENTITY. `light: eos` looks right and is
# WRONG -- `eos` is a complete DARK palette now, so it would paint its dark ramp
# into the light scheme: dark-on-light, no error anywhere. The light row of that
# identity is `eos-light`. theme.py reports this by name if it happens again, but
# the reason it is worth reading here is that the mistake LOOKS like a working
# line of config.
#
# Both are CANONICAL themes -- values live in mawizorek/maw-themes and are
# vendored, byte-verified, into theme/canonical/. Every canonical slug is
# selectable here alongside the local ones in theme/themes.tsv.
#
# WARNING: TYPOGRAPHY DOES NOT SPLIT. Type is scheme-independent by design, so
# one theme supplies it and it is the DARK one. `mclaren` declares no typography
# rows of its own, so eos's monospace data font is NOT applied on this site any
# more. The build names what it dropped.
theme:
  dark: mclaren
  light: eos-light

# THE LAST DOOR BACK TO THE SOURCE.
#
# `true` keeps ONE quiet line at the foot of each page: "Edit this page on
# GitHub". Nothing else in the rendered site mentions a repository -- the
# header widget is gone and `repo_url` is never set.
#
# Set this to `false` and the site stops referring to git entirely. That is the
# intended end state once these go out to designers, guest artists and
# students: the line is scaffolding for now, not a feature of a finished site.
# One word, per site, no code change, no rebuild of anything else.
edit_links: true

# WARNING: THE `palette:` BLOCK IS GONE (2026-08-04). This note replaces it so
# nobody puts it back.
#
# It set Material's header and accent to NAMED colours (`black`, `amber`) and
# carried the comment "Kept in step with the eos accent by hand." That was the
# hand mirroring, admitted in writing, and it went out of step the moment the
# palette became canonical.
#
# It also cost more than a colour. Setting `palette.primary` is what makes
# Material emit `data-md-color-primary`, and Material's own compound rule
# [data-md-color-scheme="slate"][data-md-color-primary="black"] then outranked
# our link colour and painted every dark-mode link blue -- for as long as this
# site has had a black header, silently, because an amber link under an amber
# accent looked correct.
#
# Chrome now resolves from the theme's canonical tokens in assets/base.css. The
# light/dark TOGGLE is unaffected: it lives in mkdocs.yml and is a feature, not
# a colour. Re-adding a `palette:` key here does nothing, and is reported.

# Titles here are a FALLBACK. A folder that contains an index.md takes that
# page's own title instead, so `venues/spac/` shows as "Swan Auditorium" rather
# than "Spac". Only folders with no index page need naming here.
sections:
  venues:
    order: 10
  production:
    order: 20
  safety:
    order: 30
  reference:
    order: 40

# The cross-site contract, pointed at a site that is ACTUALLY LIVE. This is the
# first real test of `@peer:id` against a published doc-index.json rather than
# against a hypothetical.
peers:
  template: https://mawizorek.github.io/doc-render-engine/

# Full leak scan, no opt-out. "uritp" is a proper noun that appears nowhere in
# the engine's code, objects, theme or assets, so the check is both safe to run
# and genuinely meaningful here -- unlike `template` and `theatre`, whose slugs
# are ordinary words. This instance is the one that actually proves the engine
# is site-agnostic.
