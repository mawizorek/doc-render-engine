"""Stage 04b -- routers. Type a key, get sent somewhere.

After links (03), because a router resolves its destinations through the same
page-id map, and that map is only populated once links has run.
"""

from docrender.router import on_page_content  # noqa: F401
